# GPT Golden Dataset

For each `TCxx.json`:
1. Run the prompt in your Custom GPT.
2. Copy only the final Steps JSON `steps` array output.
3. Paste into `steps`.
4. Add any anomalies in `notes`.

Keep schema unchanged.
